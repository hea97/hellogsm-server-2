version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/aws-study
    overwrite: yes

permissions:
  - object: /home/ec2-user/aws-study
    pattern: "**"
    owner: ec2-user
    group: ec2-user
    
hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: ec2-user


